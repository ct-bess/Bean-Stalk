import { MessageActionRow, MessageButton } from 'discord.js';
import Event from '../struct/Event';
import Constants from '../util/constants';

class Geefa extends Event {

  constructor() {
    super();
  }



}

/**
 * send the mf like button at 4:19pm, then activate it at 4:20pm
 * @param {TextChannel} channel
export const geefa = ( channel ) => {

  const actionRow = [ new MessageActionRow() ];

  actionRow[0].addComponents([ new MessageButton({
    label: "geefa",
    style: Constants.styles.SECONDARY,
    customId: "event-geefa",
    disabled: true
  }) ]);

}
 */

/**
 * @typedef {import('discord.js').TextChannel} TextChannel
 */