export default {
  name: "template",
  aliases: [],
  description: "template",
  exec( message, bot ) {
    const args = message.content.slice( 1 ).split( /\s+/ );
    message.channel.send( "template" );
  }
};
