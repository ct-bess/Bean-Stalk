{
  "name": "beanstalk",
  "version": "1.0.0",
  "description": "Discord Bot",
  "main": "index.js",
  "scripts": {
    "init": "echo '[]' > events.json && echo '{}' > guild.json",
    "bundle": "babel src -d lib",
    "start": "node lib/index.js",
    "dab": "npm run bundle && npm run start",
    "test": "babel src/test.js -o lib/test.js && node lib/test.js",
    "clean": "rm -r lib/"
  },
  "keywords": [
    "bot"
  ],
  "author": "connor",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/core": "^7.12.3",
    "@babel/preset-env": "^7.12.1",
    "discord.js": "^12.4.1"
  },
  "dependencies": {
    "@discordjs/opus": "^0.4.0",
    "ffmpeg-static": "^4.2.7",
    "opusscript": "^0.0.8"
  }
}
